<Page
    x:Class="LotteryApp.Pages.Customer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LotteryApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- column 0, row 0, customer heading, merges two columns-->
        <Border Background="#2f5cb6" Grid.ColumnSpan="2"/>
        <!-- column 0, row 0-->
        <!-- column 0, row 1, customer name-->
        <Border Grid.Row ="1" Background="#1f3d7a"/>
        <!-- column 0, row 1, customer phone-->
        <Border Grid.Row ="2" Background="#1f3d7a"/>
        <!-- column 0, row 2, customer email-->
        <Border Grid.Row="3" Background="#152951"/>
        <!-- column 0/1 row 0-->
        <TextBlock Foreground="White" FontSize="25" Text="Customer"/>
        <TextBlock Grid.Row="1" Foreground="White" FontSize="25" Text="Name"/>

    </Grid>
</Page>
